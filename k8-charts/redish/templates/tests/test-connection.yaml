apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "redish.fullname" . }}-test-connection"
  labels:
    {{- include "redish.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "redish.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
