apiVersion: v1
kind: Service
metadata:
  name: {{ include "ordersh.fullname" . }}
  labels:
    {{- include "ordersh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ordersh.selectorLabels" . | nindent 4 }}
