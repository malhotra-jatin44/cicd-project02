apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "catalogh.fullname" . }}-test-connection"
  labels:
    {{- include "catalogh.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "catalogh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
