apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgresh.fullname" . }}-test-connection"
  labels:
    {{- include "postgresh.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postgresh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
