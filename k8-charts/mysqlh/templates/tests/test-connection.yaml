apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mysqlh.fullname" . }}-test-connection"
  labels:
    {{- include "mysqlh.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mysqlh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
