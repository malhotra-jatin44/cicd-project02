apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysqlh.fullname" . }}
  labels:
    {{- include "mysqlh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mysqlh.selectorLabels" . | nindent 4 }}
