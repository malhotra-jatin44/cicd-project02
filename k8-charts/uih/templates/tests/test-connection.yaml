apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uih.fullname" . }}-test-connection"
  labels:
    {{- include "uih.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uih.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
